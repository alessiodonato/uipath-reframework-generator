<?xml version="1.0" encoding="utf-8"?>
<!--
  InitAllApplications.xaml
  Opens all applications used in {{PROCESS_NAME}}: {{APPLICATIONS_LIST}}
  Generated by ReFramework Generator v{{VERSION}} on {{CURRENT_DATE}}
-->
<Activity
  mc:Ignorable="sap sap2010"
  x:Class="UiPath.ReFramework.Framework.InitAllApplications"
  xmlns="http://schemas.microsoft.com/netfx/2009/xaml/activities"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:sap2010="http://schemas.microsoft.com/netfx/2010/xaml/activities/presentation"
  xmlns:scg="clr-namespace:System.Collections.Generic;assembly=mscorlib"
  xmlns:ui="http://schemas.uipath.com/workflow/activities"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <x:Members>
    <x:Property Name="in_Config" Type="InArgument(scg:Dictionary(x:String, x:Object))"
      sap2010:Annotation.AnnotationText="Configuration dictionary with URLs and credential asset names." />
  </x:Members>
  <Sequence
    sap2010:Annotation.AnnotationText="Open all applications for {{PROCESS_NAME}}.&#xA;Applications: {{APPLICATIONS_LIST}}&#xA;Each open is delegated to Business\Open{AppName}.xaml"
    DisplayName="Init All Applications">

    <ui:LogMessage Level="Trace"
      Message="'[{{PROCESS_NAME}}] Initializing applications: {{APPLICATIONS_LIST}}'"
      DisplayName="Log: Initializing apps" />

    <!-- ════════════════════════════════════════════════════════════ -->
    <!-- APPLICATION INIT STUBS — one per application from PDD       -->
    <!-- ════════════════════════════════════════════════════════════ -->

{{APPLICATION_STUBS_PLACEHOLDER}}

    <!-- ════════════════════════════════════════════════════════════ -->

    <ui:LogMessage Level="Trace"
      Message="'[{{PROCESS_NAME}}] All applications initialized and ready.'"
      DisplayName="Log: All apps ready" />

  </Sequence>
</Activity>
